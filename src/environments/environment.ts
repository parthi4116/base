// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiUrl: 'api',
  firebase: {
    apiKey: "AIzaSyDkPqjinh9Ed_I2t4AQu9lEpvyBcr-bz8U",
    authDomain: "base-project-801de.firebaseapp.com",
    projectId: "base-project-801de",
    storageBucket: "base-project-801de.appspot.com",
    messagingSenderId: "299114110245",
    appId: "1:299114110245:web:adad95b6873046a0628ecc",
    measurementId: "G-MNZMJ7W08S"
  }

};


// Initialize Firebase
// const app = initializeApp(firebaseConfig);
// const analytics = getAnalytics(app);

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
